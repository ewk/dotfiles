# Always sort the files
--sort-files

# Color is on by default. Setting it here confuses piped output.
#--color

# Use "less" as my pager
#--pager=less -R

# Example: type addition; treat .xs as a perl file
# --type-add=perl:ext:xs
# Example: define a new type
# --type-set=eiffel:ext:e,eiffel

